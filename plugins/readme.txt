Before compiling these plugins you must have generated the
System Informer SDK. You can do this by running build_init.cmd
in the build directory. Alternatively, you can open and build
the Plugins.sln solution directly in Visual Studio.